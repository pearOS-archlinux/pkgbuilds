# Maintainer: Alexandru Balan <alex@pear-software.com>

pkgname=pear-temp-fix
pkgver=26.8
pkgrel=1
pkgdesc="libprocesscore.so.10 -> .so.11 patcher and [cachyos] repository enabler"
arch=('x86_64')
url="https://github.com/pearOS-archlinux/iso/"
license=('MIT')
depends=('cachyos-keyring')
install="${pkgname}.install"

package() {
    return 0
}
